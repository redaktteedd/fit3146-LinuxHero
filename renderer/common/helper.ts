/**
 * Function to format for terminal output
 * @param lines Array of strings to format
 * @returns formatted string with new lines
 */
export function formatOutput(lines: string[]): string {
    return lines.join('\n');
}